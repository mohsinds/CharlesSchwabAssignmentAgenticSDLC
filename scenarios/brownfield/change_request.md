# Brownfield change request

Add rate limiting (token bucket, 30 req/min per IP) and a Redis cache for short-code lookups to the existing URL shortener in `seed_repo/`.

Preserve existing API contracts. Document migration notes.
