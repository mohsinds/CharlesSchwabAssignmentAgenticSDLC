# Expected artifacts (brownfield)

- Ingest report with chunk counts from `seed_repo`
- Design citing retrieved modules (`app.py`, store)
- Implementation deltas: rate limiter middleware + Redis cache adapter
- Updated tests for rate-limit 429 behavior
